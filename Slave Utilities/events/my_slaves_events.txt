namespace = my_slaves

event = {
    id = my_slaves.001
    is_triggered_only = yes

    immediate = {
        gui_open_window = my_slaves_window
    }
}

event = {
    id = my_slaves.100
    is_triggered_only = yes

    immediate = {
        remove_character_flag = slave
    }
}