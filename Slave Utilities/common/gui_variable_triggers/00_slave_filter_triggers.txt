slave_filter_is_all = {
    variable = slave_filter
    value = all
}

slave_filter_is_adult = {
    variable = slave_filter
    value = adult
}

slave_filter_is_child = {
    variable = slave_filter
    value = child
}
